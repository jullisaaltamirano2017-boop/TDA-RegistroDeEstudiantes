```mermaid
classDiagram
    class estudiante {
        -String id
        -String nombre
        -double promedio
        +estudiante(String id, String nombre, double promedio)
        +getId() String
        +getNombre() String
        +getPromedio() String
        +setNombre(String nombre) void
        +setPromedio(double promedio) void
        +toString() String
    }

    class registroEstudiantes {
        -estudiante[] lista
        -int cantidad
        -int MAX
        +registroEstudiantes()
        +getCantidad() int
        +getCapacidadMax() int
        +agregarEstudiante(estudiante est) boolean
        +buscarPorId(String id) estudiante
        +modificarPromedio(String id, double nuevoPromedio) boolean
        +eliminarEstudiante(String id) boolean
        +listarEstudiantes() void
        +cargarAlumnosIniciales() void
    }

    class main {
        +main(String[] args)*
    }

    registroEstudiantes o-- estudiante
    main ..> registroEstudiantes
